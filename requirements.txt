# MCP SDK
mcp[cli]>=1.2.0

# HTTP client
httpx>=0.27.0

# Web framework (for streamable HTTP transport)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Browser automation (CodeChef interaction)
playwright>=1.40.0

# Environment variables
python-dotenv>=1.0.0

# HTML parsing (strip HTML from problem statements)
beautifulsoup4>=4.12.0

# Database ORM
sqlalchemy>=2.0.0
aiosqlite>=0.19.0

# Caching (optional — falls back to in-memory)
redis>=5.0.0

# Docker SDK (sandbox execution)
docker>=7.0.0

# Logging
structlog>=24.0.0
